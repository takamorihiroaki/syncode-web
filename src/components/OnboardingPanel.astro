<section class="onboarding-section" id="onboarding-panel">
  <div class="onboarding-container">
    <div class="onboarding-header">
      <span class="onboarding-badge">DISCOVERY GATEWAY</span>
      <h2 class="onboarding-title">AIとの共創を、次の次元へ。</h2>
      <p class="onboarding-subtitle">あなたの現在地に合わせた、最適な知見への入口をご案内します。</p>
    </div>

    <div class="onboarding-grid">
      <!-- 初めての方 -->
      <a href="/articles/antigravity-start/" class="onboarding-card card-start">
        <div class="card-body">
          <div class="card-meta">
            <span class="card-number">01</span>
            <span class="card-phase">BEGINNER</span>
          </div>
          <h3 class="card-heading">AIエージェントの<br/>基礎を構築する</h3>
          <p class="card-desc">環境構築から最初の成功体験まで。30年の現場知見を凝縮した、5分で読めるスタートガイド。</p>
          <div class="card-action">
            <span class="action-label">VIEW START GUIDE</span>
            <span class="action-arrow">→</span>
          </div>
        </div>
      </a>

      <!-- 使い始めた方 -->
      <a href="/antigravity/hub/" class="onboarding-card card-intermediate">
        <div class="card-body">
          <div class="card-meta">
            <span class="card-number">02</span>
            <span class="card-phase">SKILL UP</span>
          </div>
          <h3 class="card-heading">実践知を体系化し<br/>指揮能力を高める</h3>
          <p class="card-desc">START → USE → FIX → MASTER。AIとの対話を4フェーズで深化させる学習ロードマップ。</p>
          <div class="card-action">
            <span class="action-label">EXPLORE ROADMAP</span>
            <span class="action-arrow">→</span>
          </div>
        </div>
      </a>

      <!-- 壁にぶつかっている方 -->
      <a href="#diagnosis-hub" class="onboarding-card card-troubleshoot">
        <div class="card-body">
          <div class="card-meta">
            <span class="card-number">03</span>
            <span class="card-phase">FIX NOW</span>
          </div>
          <h3 class="card-heading">発生している問題を<br/>即座に解決する</h3>
          <p class="card-desc">「重い」「コードが破壊される」「文脈を忘れる」。症状から最適な解決プロトコルを提示します。</p>
          <div class="card-action">
            <span class="action-label">START DIAGNOSIS</span>
            <span class="action-arrow">→</span>
          </div>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  .onboarding-section {
    padding: 100px 0 120px;
    background: #fff;
  }

  .onboarding-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 5%;
  }

  .onboarding-header {
    text-align: center;
    margin-bottom: 80px;
  }

  .onboarding-badge {
    display: inline-block;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.5em;
    text-transform: uppercase;
    color: #666;
    margin-bottom: 24px;
  }

  .onboarding-title {
    font-family: var(--font-heading);
    font-size: 2.2rem;
    font-weight: 800;
    color: #000;
    letter-spacing: -0.01em;
    margin-bottom: 16px;
  }

  .onboarding-subtitle {
    font-size: 1rem;
    color: #888;
    max-width: 600px;
    margin: 0 auto;
  }

  .onboarding-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
  }

  .onboarding-card {
    position: relative;
    display: flex;
    flex-direction: column;
    background: #fff;
    border: 1px solid rgba(0,0,0,0.06);
    text-decoration: none;
    transition: all 0.6s var(--ease-out-expo);
  }

  /* シグネチャー：L字ボーダー */
  .onboarding-card::before, .onboarding-card::after {
    content: '';
    position: absolute;
    width: 20px;
    height: 20px;
    border: 1px solid rgba(0,0,0,0.15);
    opacity: 0;
    transition: all 0.4s var(--ease-out-expo);
    pointer-events: none;
    z-index: 10;
  }
  .onboarding-card::before { top: -12px; left: -12px; border-right: none; border-bottom: none; }
  .onboarding-card::after { bottom: -12px; right: -12px; border-left: none; border-top: none; }

  .onboarding-card:hover {
    transform: translateY(-10px);
    border-color: #000;
    box-shadow: 0 30px 60px rgba(0,0,0,0.08);
  }

  .onboarding-card:hover::before, .onboarding-card:hover::after {
    opacity: 1;
    width: 30px;
    height: 30px;
  }

  .card-body {
    padding: 60px 40px;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }

  .card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 40px;
  }

  .card-number {
    font-family: var(--font-heading);
    font-size: 0.8rem;
    font-weight: 800;
    color: #ddd;
    letter-spacing: 0.1em;
    transition: color 0.4s ease;
  }

  .onboarding-card:hover .card-number {
    color: #000;
  }

  .card-phase {
    font-size: 0.6rem;
    font-weight: 800;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: #999;
  }

  .card-heading {
    font-family: var(--font-heading);
    font-size: 1.4rem;
    font-weight: 700;
    line-height: 1.5;
    color: #000;
    margin: 0 0 24px 0;
    letter-spacing: 0.01em;
  }

  .card-desc {
    font-size: 0.9rem;
    line-height: 2;
    color: #666;
    margin: 0;
    flex-grow: 1;
    font-weight: 400;
  }

  .card-action {
    margin-top: 48px;
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .action-label {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    font-weight: 800;
    color: #000;
    letter-spacing: 0.2em;
  }

  .action-arrow {
    font-size: 1.1rem;
    transition: transform 0.4s var(--ease-out-expo);
  }

  .onboarding-card:hover .action-arrow {
    transform: translateX(8px);
  }

  @media (max-width: 1024px) {
    .onboarding-grid {
      gap: 20px;
    }
    .card-body {
        padding: 40px 30px;
    }
  }

  @media (max-width: 900px) {
    .onboarding-grid {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .card-body {
      padding: 40px;
    }
  }

  @media (max-width: 480px) {
    .onboarding-title {
      font-size: 1.6rem;
    }
    .card-body {
      padding: 30px;
    }
  }
</style>

