<section class="gateway-section">
  <div class="gateway-container">
    <div class="gateway-grid">
      <!-- LEFT: PRACTICAL GATEWAY -->
      <a href="/articles/antigravity-start/" class="gateway-card">
        <div class="gateway-card__content">
          <div class="gateway-meta">
            <span class="gateway-label">PRACTICAL GUIDE</span>
          </div>
          <h3 class="gateway-title">Antigravity<br/>導入・スタートプロトコル</h3>
          <p class="gateway-desc">
            環境構築から最初の一歩まで。<br/>
            30年の現場知見を凝縮した、最短で成果を出すための実戦実装ガイド。
          </p>
          <div class="gateway-footer">
            <span class="gateway-action">INITIALIZE START</span>
            <span class="gateway-arrow">→</span>
          </div>
        </div>
      </a>

      <!-- RIGHT: EXPERTISE GATEWAY -->
      <a href="/antigravity/hub/" class="gateway-card">
        <div class="gateway-card__content">
          <div class="gateway-meta">
            <span class="gateway-label">ROADMAP HUB</span>
          </div>
          <h3 class="gateway-title">Antigravity<br/>完全攻略ナレッジ・ハブ</h3>
          <p class="gateway-desc">
            散在する知見を4フェーズで体系化。<br/>
            STARTからMASTERまで、AIを指揮する能力を深化させるナビゲーション。
          </p>
          
          <div class="gateway-quick-nav">
            <object><a href="/antigravity/hub/#phase-start" class="quick-link-item">START</a></object>
            <object><a href="/antigravity/hub/#phase-use" class="quick-link-item">USE</a></object>
            <object><a href="/antigravity/hub/#phase-fix" class="quick-link-item">FIX</a></object>
            <object><a href="/antigravity/hub/#phase-master" class="quick-link-item">MASTER</a></object>
          </div>

          <div class="gateway-footer">
            <span class="gateway-action">ENTER SYSTEM HUB</span>
            <span class="gateway-arrow">→</span>
          </div>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  .gateway-section {
    padding: 0 0 120px;
    background: #fff;
    position: relative;
    z-index: 10;
  }

  .gateway-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 5%;
  }

  .gateway-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 40px;
  }

  .gateway-card {
    position: relative;
    display: block;
    padding: 80px 50px;
    text-decoration: none;
    background: #fff;
    border: 1px solid rgba(0, 0, 0, 0.05);
    transition: all 0.6s var(--ease-out-expo);
  }

  /* シグネチャー：L字ボーダー */
  .gateway-card::before, .gateway-card::after {
    content: '';
    position: absolute;
    width: 24px;
    height: 24px;
    border: 1px solid rgba(0,0,0,0.15); /* 浮かせるので、少し繊細な線に */
    opacity: 0;
    transition: all 0.4s var(--ease-out-expo);
    pointer-events: none;
    z-index: 10;
  }
  .gateway-card::before { top: -12px; left: -12px; border-right: none; border-bottom: none; }
  .gateway-card::after { bottom: -12px; right: -12px; border-left: none; border-top: none; }

  .gateway-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 30px 60px rgba(0, 0, 0, 0.06);
    border-color: #000;
  }

  .gateway-card:hover::before, .gateway-card:hover::after {
    opacity: 1;
    width: 30px;
    height: 30px;
  }

  .gateway-card__content {
    position: relative;
    z-index: 2;
  }

  .gateway-meta {
    margin-bottom: 30px;
  }

  .gateway-label {
    display: block;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    color: #999;
  }

  .gateway-title {
    font-family: var(--font-heading);
    font-size: 1.8rem;
    font-weight: 800;
    line-height: 1.35;
    margin-bottom: 24px;
    letter-spacing: -0.01em;
    color: #000;
  }

  .gateway-desc {
    font-size: 0.9rem;
    line-height: 2;
    color: #666;
    margin-bottom: 40px;
    max-width: 420px;
  }

  .gateway-footer {
    display: flex;
    align-items: center;
    gap: 12px;
    padding-top: 24px;
    border-top: 1px solid rgba(0,0,0,0.03);
  }

  .gateway-quick-nav {
    display: flex;
    gap: 8px;
    margin-bottom: 32px;
    flex-wrap: wrap;
    position: relative;
    z-index: 5; /* カードのリンクより上に配置 */
  }

  /* objectタグが巨大な透明ブロックになるのを防ぐ */
  .gateway-quick-nav object {
    display: inline-block;
    vertical-align: middle;
  }

  .quick-link-item {
    font-size: 0.6rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    color: #888;
    text-decoration: none;
    padding: 6px 16px;
    border: 1px solid rgba(0,0,0,0.05);
    transition: all 0.3s var(--ease-out-expo);
    display: inline-block;
  }

  .quick-link-item:hover {
    background: #000;
    color: #fff;
    border-color: #000;
  }

  .gateway-action {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    color: #000;
  }

  .gateway-arrow {
    font-size: 1rem;
    color: #bbb;
    transition: all 0.4s var(--ease-out-expo);
  }

  .gateway-card:hover .gateway-arrow {
    transform: translateX(8px);
    color: #000;
  }

  @media (max-width: 900px) {
    .gateway-grid {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .gateway-card {
      padding: 60px 40px;
    }
    .gateway-title {
      font-size: 1.6rem;
    }
  }

  @media (max-width: 480px) {
    .gateway-card {
      padding: 40px 30px;
    }
  }
</style>

