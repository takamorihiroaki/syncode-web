# 記事の内容と画像の復元プラン（4月5日版）

現状、修復スクリプト（`index.js`）の解析ロジックに不備があり、本文中に `---` などの区切り線が含まれる場合に、その後の文章を誤って削除してしまっています。その結果、4月5日時点で存在していた充実した内容（「実験内容」や「手順」の詳細など）が消失しています。

これを解消し、バックアップから **「4月5日時点の画像と完全な本文」** を復元します。

## ユーザー確認事項

> [!IMPORTANT]
> - 復元対象の記事は **「【初心者向け】AntigravityのBrowser Controlが動かない？...」** （`antigravity-browser-control-guide.md`）の充実した本文と、そのバックアップに記載されていた画像パスであることを想定しています。
> - 画像パスについて、バックアップには `/assets/antigravity-browser-control-main.png` と記載されています。これを優先して復元します。

## 提案される変更点

### 1. 修復スクリプト（index.js）のバグ修正
- 本文のクリーンアップ処理を修正し、`---` で囲まれたブロックを「フロントマター形式のメタデータのみ」に限定して除去するようにします。
- 本文のテキスト（`## 章タイトル` など）が誤って削除されるのを防ぎます。

### 2. バックアップからの完全な復元
- `src/articles_backup/` に保存されているオリジナルの全テキストを読み込み、正常なフロントマター（`date`, `image`, `excerpt`）を付与した上で、本文を丸ごと復元します。

## 実施計画

1. **スクリプトの修正**: `index.js` の `cleanAndParse` 関数を、本文を保護するように書き換えます。
2. **復元の実行**: `node scripts/cms-repair/index.js` を再実行し、バックアップから全データを再インポートします。
3. **内容の確認**: `src/content/articles/antigravity-browser-control-guide.md` を開き、行数が150行程度に増えていることを確認します。
4. **表示の確認**: ブラウザツールで、404エラーが出ず、かつ内容が欠落していないことを最終確認します。

## 動作確認方法

### 自動テスト
- `npx astro check` でエラーが出ないことを確認。

### 手動確認
- エディタで記事を開き、「導入」以降のセクション（実験内容、結果と考察など）がすべて存在することを確認。
